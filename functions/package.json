{
  "name": "primelaze-messaging-functions",
  "description": "Cloud Functions for the Primelaze dashboard — sends WhatsApp / SMS dispatch updates to Casovil lead customers.",
  "version": "1.0.0",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  },
  "private": true
}
